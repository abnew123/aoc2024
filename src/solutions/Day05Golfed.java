import java.util.*;class E {boolean[][] b = new boolean[100][100];String s(boolean P,String[]S) {long A = 0;for(String l:S){if (l.contains("|")) b[Integer.parseInt(l.substring(0, 2))][Integer.parseInt(l.substring(3))] = true;else if (l.contains(",")) {var u= Arrays.stream(l.split(",")).map(Integer::parseInt).toArray(Integer[]::new);boolean o = o(u);if (P == o) {if (!o) Arrays.sort(u, (x,y)->b[x][y]?-1:b[y][x] ? 1 : 0);A += u[u.length / 2];}}}return "" + A;}boolean o(Integer[] u) {for (int i = 0; i < u.length; i++) for (int j = i + 1; j < u.length; j++) if (b[u[j]][u[i]]) return 1<0;return 1>0;}}
